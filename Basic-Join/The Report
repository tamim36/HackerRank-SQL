SELECT
    CASE
        WHEN G.Grade < 8 THEN 'NULL'
        ELSE S.Name
    END AS Name,
    G.Grade,
    S.Marks
FROM Students S
CROSS JOIN Grades G
WHERE 
    S.Marks BETWEEN G.Min_Mark AND G.Max_Mark
ORDER BY G.Grade DESC, S.Name;